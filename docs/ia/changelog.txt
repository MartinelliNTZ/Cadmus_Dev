#inicie aqui sempre use a data atual nos logs sempre insira na sequencia
2.3.29 27/04/2026
[2.3.29.1][27/04/2026] comece aqui
[2.3.29.2][28/04/2026] Corrigido possível erro de argumentos duplicados em DividePointsByStripsPlugin.judge_args
[2.3.29.3][28/04/2026] Atualizado DividePointsByStripsPlugin para gerar shot_id como string com prefixo de grupo e linha de strip com geometry robusta
[2.3.29.4][28/04/2026] Adicionados logs de validação em _generate_strip_lines_layer e processamento de grupos para debug de linhas zeradas
[2.3.29.5][28/04/2026] Refatoração v3 do SequentialPointBreakJudge:
  - _precompute_metrics agora retorna 3 valores (raw_az, smoothed_az, metrics) — raw_az contém azimutes brutos (não suavizados) para a checagem bidirecional detector inflexões reais sem falsos negativos.
  - raw_az[0] = None em vez de 0.0; az_history começa vazio e add_to_history rejeita None/0.0, eliminando a poluição do histórico inicial com azimute fantasma.
  - _bidirectional_inflection_check recebe raw_az (bruto), não smoothed_az — a suavização prévia diluía gradientes de direção e produzia falsos negativos de quebra.
  - Fusão de shots (_fuse_small_shots) recalcula tamanhos incrementalmente com loop while + set fused_from, corrigindo fusões em cadeia que usavam estatísticas obsoletas.
  - _apply_retroactive_relabel_guarded verifica se o shot de origem atingiu minimum_point_count antes de permitir relabel, evitando destruir strips válidas ao retirar seus pontos de cauda.
  - Duplo check de azimute anterior+posterior: para cada ponto i, compara raw_az[i] (segmento i-1→i) e raw_az[i+1] (segmento i→i+1) contra a média da strip. Se AMBOS desviam > severe_azimuth_threshold → dual_angle_break: +2 no score, quebra antecipada (R0.5), sobrepõe falsos positivos da checagem bidirecional, reduz confirmações prospectivas necessárias, transforma ambíguo em quebra, relaxa guard do relabel retroativo (janela dobrada + threshold severo). Se ambos desviam > light_azimuth_threshold → dual_angle_light: +1 score, reduz required_conf. Também estende detecção de bordadura para dual_angle_break com distância < 2× border_distance_threshold.

2.3.30 09/06/2026
[2.3.30.1][09/06/2026] Consolidada documentação da skill de comunicação (SignalHub / PyQtSignalManager / QgisMessageUtil) em docs/skills/SKILL_COMUNICATION_SYSTEM.md — inclui padrões de payload, exemplos de uso, integração com LogUtils e recomendações de segurança/threads.
2.3.31 09/06/2026
[2.3.31.1][09/06/2026] Reestruturada skill de comunicação: clarificados cenários de uso para LogUtils, PluginSignalHub/PyQtSignalManager e QgisMessageUtil; adicionadas políticas sobre `tool_key`, payloads, responsabilidades e testes.
2.3.32 09/06/2026
[2.3.32.1][09/06/2026] Ajuste da skill de comunicação: `tool_key` declarado exclusivo para ferramentas; classes não-ferramentas devem receber `tool_key` como parâmetro ou receber `logger` injetado. Exemplos padronizados atualizados.
2.3.33 08/07/2026
[2.3.33.1][08/07/2026] SKILL_AGENT.md aprimorado com seção MAPEAMENTO SITUAÇÃO → SKILL, referenciando todas as 12 skills complementares; atualizado workflow PASSO 1, DIRETIVAS CENTRAIS, REGRAS OPERACIONAIS, ERROS COMUNS e seção QUANDO USAR/NÃO USAR com referências cruzadas às skills corretas.
2.3.34 08/07/2026
[2.3.34.1][08/07/2026] ReverseGeocodeTask padronizado para contrato pipeline:
  - Herda de BaseTask (não mais QgsTask direto)
  - tool_key obrigatório como keyword-only argument
  - Método _run() em vez de run() (BaseTask gerencia wrapper)
  - Remove callback pattern legado (on_success/on_error agora são callbacks reservados da engine)
  - Cancelamento cooperativo via isCanceled()
  - LogUtils usa tool_key da task (não string hardcoded)
[2.3.34.2][08/07/2026] ReverseGeocodeStep padronizado para contrato pipeline:
  - should_run() retorna condicional baseado em lat/lon no contexto (não retorna None)
  - create_task() sempre retorna ReverseGeocodeTask (nunca None)
  - on_success() persiste resultado no contexto via context.set("address_data")
  - Import corrigido de ..task.reverse_geocoding_task para ..task.ReverseGeocodeTask
[2.3.34.3][08/07/2026] CoordClickTool fallback atualizado para nova assinatura ReverseGeocodeTask(lat, lon, tool_key=...)
2.3.35 09/07/2026
[2.3.35.1][09/07/2026] Refatoração ExecutionContext + Steps:
  - ExecutionContext agora com atributos canônicos (input_path, output_path, files, tool_key, json_path)
  - Mantida compatibilidade total com set()/get() legado (DEPRECATED) para plugins não refatorados
  - Adicionado set_result()/get_result() para comunicação entre steps
  - json_path promovido a atributo canônico (usado por múltiplos steps)
[2.3.35.2][09/07/2026] Steps refatorados com __init__ parametrizado (sem dependência do context):
  - PhotoEnrichmentStep: source, enable_mrk, enable_exif, enable_xmp, enable_custom_fields, selected_*_fields, project_title, logo_path, recursive, paths
  - JsonVectorizationStep: source, layer_name
  - ReportGenerationStep: html_output_path
  - Todos os steps resolvem tool_key, input_path do context canônico (nunca via context.get())
[2.3.35.3][09/07/2026] Plugins 100% convertidos para novo padrão (sem contexto legado):
  - DroneCoordinates.py: executa pipeline com steps parametrizados, context só com atributos canônicos
  - DroneCoordinatesRunner.py: executa pipeline com steps parametrizados, context só com atributos canônicos
  - PhotoVectorizationPlugin.py: executa pipeline com steps parametrizados, context só com atributos canônicos
  - Nenhum context.set() ou context.get() para configuração de steps — tudo via parâmetros nomeados
[2.3.35.4][09/07/2026] Skills atualizadas com novo padrão:
  - SKILL_ASYNC_PAPELINE.md: seção 3 (Step parametrizado), seção 4 (ExecutionContext canônico), seção 18-19 (exemplos + resumo)
  - SKILL_METADATA_PIPELINE.md: versão 3.0.0 — exemplos migrados para novo padrão, tabelas de parâmetros dos steps
  - changelog.txt registrado como 2.3.35.4
2.3.36 09/07/2026
[2.3.36.1][09/07/2026] MRK como agregado opcional, coordenadas SEMPRE das fotos:
  - DroneCoordinates: adicionado checkbox "Obter dados MRK" (default: True para compatibilidade retroativa)
  - DroneCoordinates._on_use_mrk_changed(): habilita/desabilita seção MRK Fields
  - DroneCoordinates._resolve_track_group_fields(): fallback sem MRK (agrupa por FolderLevel1)
  - PhotoMetadata._enrich_with_mrk(): NÃO seta mais CoordSource=MRK nem QUALITY_FLAG=OK
  - JsonToVectorTranslator._resolve_geometry(): remove fallback Lat/Lon (MRK)
  - DroneCoordinatesRunner: respeita preferência use_mrk das prefs
  - i18n: nova string USE_MRK_DATA em pt_BR, en, es, de
  - SKILL_METADATA_PIPELINE.md: v3.1.0 — MRK como agregado
[2.3.36.2][09/07/2026] Correção DroneCoordinatesRunner — defaults e campos alinhados com DroneCoordinates (UI):
  - apply_photos default alterado de False para True (mesmo do UI)
  - adicionado is_recursive lido das prefs (default True), antes era False hardcoded
  - recursive no PhotoEnrichmentStep agora usa is_recursive das prefs
  - selected_required_fields agora carrega das chaves exif_fields_selected + xmp_fields_selected
    (antes usava required_fields_selected que nunca era salva pelo UI)
2.3.37 09/07/2026
[2.3.37.1][09/07/2026] Unificação do pipeline drone em DronePipelineService:
  - Criado core/services/DronePipelineService.py — serviço único com DEFAULTS centralizado,
    método execute() e callback _on_pipeline_finished() unificado
  - MrkDropHandler simplificado: delega diretamente para DronePipelineService.execute()
  - DroneCoordinatesRunner removido — não era mais necessário (MrkDropHandler e DroneCoordinates
    chamam o Service diretamente)
  - DroneCoordinates.execute_tool() simplificado: salva prefs e chama DronePipelineService.execute()
  - Removidos métodos duplicados _resolve_track_order_field e _resolve_track_group_fields
    do DroneCoordinates (agora no Service)
  - Criado docs/plano_refatoracao_unificar_drone_pipeline.md com análise e proposta
2.3.40 09/07/2026
[2.3.40.1][09/07/2026] ReverseGeocodeStep integrado ao pipeline de drone + novo modo JSON:
  - ReverseGeocodeStep refatorado: agora aceita json_path no __init__ e extrai coordenadas
    da primeira foto do JSON (Lat/Lon), mantendo compatibilidade com modo legado (lat/lon direto)
  - Adicionado `_get_first_photo_coords()` que percorre groups/records do JSON v2.0
  - Adicionado `_resolve_coords()` com prioridade: __init__ > context > JSON
  - on_success agora persiste dados no JSON via `JsonUtil.update_geocode_data()`
    e timestamps `geocode_start`/`geocode_end` via `JsonUtil.update_timestamps()`
  - should_run() retorna False se coordenadas não disponíveis (step pulável)
[2.3.40.2][09/07/2026] JsonUtil.update_geocode_data() adicionado:
  - Abre JSON existente e adiciona `geocode.municipio`, `geocode.state`, etc. no cabeçalho
  - Usado pelo ReverseGeocodeStep.on_success() para persistir localização
[2.3.40.3][09/07/2026] DronePipelineService pipeline atualizado:
  - Pipeline agora: PhotoEnrichmentStep → ReverseGeocodeStep → JsonVectorizationStep → (ReportGenerationStep?)
  - ReverseGeocodeStep() sem parâmetros: lê json_path do context automaticamente
  - SKILL_METADATA_PIPELINE.md atualizado para v3.2.0 com novo fluxo
[2.3.40.4][09/07/2026] AltimetryStep padronizado + altimetry_task migrado para BaseTask:
  - altimetry_task.py: migrado de QgsTask direto para BaseTask, assinatura `AltimetriaTask(lat, lon, tool_key=...)`
  - AltimetryStep.py: refatorado com `__init__` parametrizado (`lat`, `lon`, `report_metadata_mode`), usa `context.tool_key` canônico
  - AltimetryStep.on_success: persiste altitude no JSON via `JsonUtil.update_geocode_data({"altitude": ...})` + timestamps altimetry_start/altimetry_end
  - CoordClickTool fallback atualizado para nova assinatura AltimetriaTask(..., tool_key=...)
  - ReverseGeocodeStep: removido json_path do __init__, adicionado report_metadata_mode, simplificado sem fallback legado
[2.3.40.5][09/07/2026] Correções de contrato (PLUGIN_CONTRACT.md):
  - BaseStep: adicionado `__init__` com `self.logger` + `_init_logger(context)` que cria LogUtils a partir de context.tool_key
  - ReverseGeocodeStep e AltimetryStep: removidos `logger = LogUtils(...)` locais → usam `self.logger` via `_init_logger(context)`
  - _get_first_photo_coords: removido `except Exception:` silencioso → `except (json.JSONDecodeError, IOError, OSError, ValueError, KeyError)` com log via `self.logger.warning()`
  - on_success: exceções logadas com `self.logger.exception(e, code="...")`
  - on_error: exceções de salvamento de timestamp logadas com `self.logger.warning()`
  - Nova utils/JsonUtil.py: `update_altitude_data()` — escreve altitude no nível raiz do JSON (fora de geocode)
2.3.41 09/07/2026
[2.3.41.1][09/07/2026] Extraída lógica de busca da primeira foto para Etapa 6 do PhotoMetadata:
  - Adicionada Etapa 6 em PhotoMetadata.run_pipeline(): percorre records enriquecidos e salva
    lat/lon da primeira foto com GPS em _first_photo_raw_coord (cache estático)
  - Adicionado PhotoMetadata.get_first_photo_raw_coord() — getter público para o cache
  - PhotoEnrichmentStep.on_success() (main thread): lê raw_coord do cache, chama
    VectorLayerProjection.get_coordinate_info() para enriquecer com EPSG, zona UTM e hemisfério,
    salva no context (first_photo_lat, first_photo_lon, first_photo_coord_info) e no JSON
    via JsonUtil.update_first_photo_coord()
  - Adicionado JsonUtil.update_first_photo_coord() — escreve dict coord_info no cabeçalho do JSON
  - ReverseGeocodeStep refatorado: remove _get_first_photo_coords(), lê first_photo_lat/lon do context
  - AltimetryStep refatorado: remove _get_first_photo_coords(), lê first_photo_lat/lon do context
  - Ambos os steps não buscam mais a primeira foto diretamente — eliminada lógica duplicada
  - SKILL_METADATA_PIPELINE.md atualizado para v3.3.0 com novo fluxo

2.3.39 09/07/2026
[2.3.39.1][09/07/2026] Refatorado DronePipelineService para usar Preferences corretamente:
  - Substituído import `from ...utils.Preferences import load_tool_prefs` (depreciado) por `from ...utils.Preferences import Preferences`
  - Substituídas todas as chamadas de `load_tool_prefs()` por `Preferences.load_tool_prefs()`
  - Substituído acesso a dict com bracket (`prefs["key"]`) por `.get("key", default)` — prevenção de KeyError
  - Afetados: `execute()`, `_on_pipeline_finished()`, `_load_safe_prefs()`
2.3.38 09/07/2026
[2.3.38.1][09/07/2026] Removida ferramenta PhotoVectorizationPlugin (plugins/PhotoVectorizationPlugin.py):
  - Deletado arquivo do plugin PhotoVectorizationPlugin.py
  - Removido ToolKey.PHOTO_VECTORIZATION de utils/ToolKeys.py
  - Removida cor TOOL_KEY_COLORS[PHOTO_VECTORIZATION] em utils/ToolKeys.py
  - Removidas strings PHOTO_VECTORIZATION_TITLE e PHOTO_VECTORIZATION_TOOLTIP de i18n/Strings_pt_BR.py, Strings_en.py, Strings_es.py, Strings_de.py
  - Removidas strings PHOTO_FOLDER, VECTORIZE_PHOTOS, PHOTOS_WITHOUT_MRK_LAYER_NAME de pt_BR, en, es, de
  - Removido registro photo_vectorization Tool de core/config/ToolRegistry.py
  - Removida constante PHOTO_VECTORIZATION de resources/IconManager.py
  - Deletado ícone resources/icons/photo_vectorization.ico
  - Nenhum outro plugin foi alterado
2.3.42 09/07/2026
[2.3.42.1][09/07/2026] lat/lon promovidos a atributos canônicos do ExecutionContext:
  - Adicionados `lat`/`lon` como atributos canônicos (type float | None) em ExecutionContext.py
  - Adicionados parâmetros `lat`/`lon` no __init__() do ExecutionContext
  - set_result()/get_result()/set()/get()/require() sincronizam com lat/lon
  - clear() reseta lat/lon para None
  - __repr__ agora exibe lat/lon
[2.3.42.2][09/07/2026] PhotoEnrichmentStep.on_success() usa context.lat/context.lon:
  - Substituído `context.set_result("first_photo_lat", ...)` e `context.set_result("first_photo_lon", ...)`
    por `context.lat = ...` e `context.lon = ...` (atributos canônicos)
  - Mantido `context.set_result("first_photo_coord_info", enrich_result)` para dados completos
[2.3.42.3][09/07/2026] AltimetryStep simplificado (sem __init__ params, sem report_metadata_mode):
  - Removeu __init__(lat, lon, report_metadata_mode) → agora __init__() vazio
  - should_run() lê context.lat/context.lon (sempre)
  - create_task() lê context.lat/context.lon (sempre)
  - on_success() persiste no JSON se context.json_path existir (independente de cenário)
  - on_error() persiste erro no JSON se context.json_path existir (independente de cenário)
[2.3.42.4][09/07/2026] ReverseGeocodeStep simplificado (sem __init__ params, sem report_metadata_mode):
  - Removeu __init__(lat, lon, report_metadata_mode) → agora __init__() vazio
  - should_run() lê context.lat/context.lon (sempre)
  - create_task() lê context.lat/context.lon (sempre)
  - on_success() persiste no JSON se context.json_path existir (independente de cenário)
  - on_error() persiste erro no JSON se context.json_path existir (independente de cenário)
[2.3.42.5][09/07/2026] CoordClickTool atualizado para novo padrão:
  - ExecutionContext criado com lat=lat, lon=lon
  - ReverseGeocodeStep() e AltimetryStep() sem parâmetros (lêem do context)
[2.3.42.6][09/07/2026] DronePipelineService atualizado:
  - ReverseGeocodeStep() e AltimetryStep() sem parâmetros (lêem do context via atributos canônicos)
  - Comentários atualizados refletindo novo fluxo
[2.3.42.7][09/07/2026] Adicionado JsonUtil.update_json() genérico:
  - Novo método estático que recebe json_path e updates dict
  - Abre JSON, aplica dict.update() no nível raiz, salva
  - Útil para steps que precisam adicionar dados simples sem criar método específico
[2.3.42.8][09/07/2026] Removidos métodos específicos de JsonUtil (unificados em update_json()):
  - Removido update_geocode_data() → substituído por update_json(json_path, {"geocode": payload})
  - Removido update_altitude_data() → substituído por update_json(json_path, {"altitude": value})
  - Removido update_first_photo_coord() → substituído por update_json(json_path, {"first_photo_coord": coord_info})
  - Callers atualizados: PhotoMetadata.enrich_first_photo_coord(), AltimetryStep.on_success(), ReverseGeocodeStep.on_success()
[2.3.42.9][09/07/2026] Skills atualizadas para v4.0.0 (lat/lon canônicos):
  - SKILL_ASYNC_PAPELINE.md: seção 4 inclui context.lat/context.lon como atributos canônicos
  - SKILL_AGENT.md: regra 3 atualizada (atributos canônicos + set_result/get_result), nova regra 4 (lat/lon)
  - SKILL_METADATA_PIPELINE.md: v4.0.0 — tabela ExecutionContext inclui lat/lon, ReverseGeocodeStep simplificado,
    histórico com entrada 4.0.0, referências a FirstPhotoCoordStep removidas
2.3.43 09/07/2026
[2.3.43.1][09/07/2026] Corrigido bug 'dict' object is not callable em PhotoMetadata.enrich_first_photo_coord():
   - Linha `raw_coord = PhotoMetadata._first_photo_raw_coord()` alterada para
     `raw_coord = PhotoMetadata._first_photo_raw_coord` (acesso a atributo, não chamada de método)
   - O erro ocorria porque _first_photo_raw_coord é um dict (ou None), não um método/função
2.3.44 09/07/2026
[2.3.44.1][09/07/2026] Adicionadas informações geoespaciais no cabeçalho do relatório HTML:
   - JsonMetadataManager.load_json_metadata() agora extrai first_photo_coord (zona_num, zona_letra, hemisferio, epsg),
     geocode (municipio, state_district, state, region, country) e altitude do JSON raiz
   - Template HTML atualizado: nova seção no cabeçalho exibindo EPSG, MUNICIPIO, DISTRITO, ESTADO,
     REGIAO, PAIS e ALTITUDE SRTM quando disponíveis no json_meta
   - SKILL_REPORT_SYSTEM.md atualizado com novos campos do json_meta
2.3.45 10/07/2026
[2.3.45.1][10/07/2026] ReverseGeocodeTask tratado como falha gracefull (não aborta pipeline):
   - TimeoutError, HTTPException, ConnectionError, OSError agora retornam result={} (sucesso)
     em vez de exception que abortava a pipeline via engine
   - HTTP não-200, redirect sem Location e dados nulos também retornam result={} (gracefull)
   - Log nivel WARNING para falhas de rede, INFO para redirects, mantendo rastreabilidade
   - Evita que pipeline de drone ou CoordClickTool seja abortada por indisponibilidade da API
2.3.46 10/07/2026
[2.3.46.1][10/07/2026] Nova ferramenta: Salvar Temporárias (SaveTemporaryLayersPlugin):
  - Reutilizado ToolKey.SAVE_TEMPORARY_LAYER existente em utils/ToolKeys.py
  - Adicionada cor SAVE_TEMPORARY_LAYER: "#3498DB" em ToolKeys.TOOL_KEY_COLORS
  - Adicionadas strings STR.SAVE_TEMPORARY_LAYER_TITLE/TOOLTIP e demais em i18n/Strings_pt_BR.py
  - Ícone SAVE_TEMPORARY_LAYER já existente em resources/IconManager.py (save_temporary_layer.ico)
  - Criado plugins/SaveTemporaryLayersPlugin.py com:
    - InputFieldsWidget para prefixo e sufixo nos nomes dos arquivos
    - DropdownSelectorWidget para extensões vetor (shp, gpkg, geojson, kml, dxf, gml, csv)
    - DropdownSelectorWidget para extensões raster (tif, jp2, png, jpg)
    - GridComplexSelector modo output sem parent, com 📁, 🛠️, ➡️
    - Import relativo corrigido: from ..resources.widgets.grid.GridComplexSelector
    - _get_temporary_layers(): percorre QgsProject.layerTreeRoot() e filtra camadas com source "memory:"
    - execute_tool(): salva vetores com QgsVectorFileWriter e rasters com QgsRasterFileWriter
    - Fallback para pasta do projeto se nenhuma saída for selecionada
    - Subpastas vectors/ e rasters/ criadas automaticamente na pasta de saída
    - Feedback ao usuário via QgisMessageUtil.bar_info/bar_warning
  - Registrado em core/config/ToolRegistry.py na categoria VECTOR com order=70,
    usando _make_plugin_executor("...plugins.SaveTemporaryLayersPlugin")
  - Removido ToolKey.FILE_CONVERTER (não utilizado)
2.3.47 10/07/2026
[2.3.47.1][10/07/2026] Atributos do vetor reordenados alfabeticamente após vetorização:
   - Adicionado import de VectorLayerAttributes em JsonVectorizationStep.run_inline()
   - Inserida chamada a VectorLayerAttributes.reorder_fields_alphabetically(layer) logo após
     criação da layer e antes de adicioná-la ao QgsProject
   - Logs: debug quando reordenação bem-sucedida, warning quando falha (ordem original mantida)
   - Utiliza método estático existente que cria nova layer em memória com campos ordenados
     case-insensitive (A-Z), preservando dados e geometria
2.3.48 11/07/2026
[2.3.48.1][11/07/2026] Novo utilitário ImageUtils:
   - Criado utils/ImageUtils.py com classe ImageUtils herdando de BaseUtil
   - Método photo_to_base64(): converte foto do disco para string base64 com data URI
     (ex: data:image/jpeg;base64,...). Suporta .jpg, .jpeg, .png, .tif, .tiff, .bmp, .gif, .webp, .ico.
     Inclui validações de arquivo, extensão, leitura segura com tratamento de exceções,
     e log via LogUtils com tool_key.
   - Método base64_to_bytes(): decodifica string base64 (com ou sem data URI) de volta para bytes.
   - Método auxiliar _guess_mime_type(): mapeia extensão para MIME type.
   - Registrado em utils/__init__.py com export ImageUtils.
    - SKILL_UTILS.md atualizado para v2.1.0: adicionada entrada #28 na tabela de classes,
      item no Resumo Executivo, caso de uso, dependência e histórico.
2.3.49 11/07/2026
[2.3.49.1][11/07/2026] Adicionado campo license_key nas configurações do SettingsPlugin:
  - InputFieldsWidget: adicionado suporte a `show_label` no field config (default True)
  - SettingsPlugin._build_ui(): inserido InputFieldsWidget sem label/tooltip para license_key
  - SettingsPlugin._load_prefs(): carrega license_key de system_preferences
  - SettingsPlugin._save_prefs(): persiste license_key em system_preferences
[2.3.49.2][11/07/2026] Criado LicenseManager em utils/LicenseManager.py:
  - Herda de BaseUtil com LogUtils e tool_key
  - Constante VALID_LICENSE_KEY = "1234" (chave válida atual)
  - Constante RENEWAL_WINDOW_DAYS = 7 (janela de renovação)
  - is_license_valid() → bool com lógica de cache e renovação:
    * Sem license_key nas prefs → False
    * Cache ativo com > 7 dias restantes → True (sem verificar)
    * 0-7 dias restantes → tenta renovar, retorna True sempre (graça)
    * Expirada → valida obrigatoriamente, retorna False se inválida
    * Sem cache → valida do zero
  - Salva license_status ("active"/"inactive") e license_expiry (YYYY-MM-DD) nas system_preferences
  - Registrado em utils/__init__.py com export LicenseManager
[2.3.49.3][11/07/2026] DronePipelineService verifica licença antes de gerar relatório:
  - Adicionado import de LicenseManager em DronePipelineService
  - Se license_key estiver configurada nas system_preferences, valida via LicenseManager.is_license_valid()
  - Se license_key NÃO estiver configurada, permite geração do relatório sem validação
  - Se license_key configurada mas inválida, suprime ReportGenerationStep com log warning
[2.3.49.4][11/07/2026] DroneCoordinates UI condicionada à licença em _build_ui():
  - LicenseManager.is_license_valid() consultado uma vez no início de _build_ui()
  - Se inválida: logo, título do projeto e generate_report NÃO são criados na UI
  - Se inválida: checkbox_options copia CHECKBOX_OPTIONS e remove "generate_report"
  - _load_prefs() e _save_prefs() usam hasattr para acesso seguro a atributos opcionais
  - Nenhum conhecimento de license_key fora do LicenseManager
2.3.50 11/07/2026
[2.3.50.1][11/07/2026] LicenseManager expandido com API pública de gerenciamento:
  - Adicionado get_license_info() → dict com has_key, key_preview, status, expiry, tier, level, is_active, days_remaining
  - Adicionado save_license_key(license_key) → dict com success/message; salva APENAS se chave válida
  - Adicionado delete_license() → remove todos os dados de licença das preferências
  - Adicionado _validate_and_get_tier() → retorna (is_valid, tier) para suporte a níveis de licença
  - Adicionado _tier_to_label() → converte tier code para label amigável (Básico, Profissional, Enterprise)
  - Constantes LICENSE_TIER_BASIC, LICENSE_TIER_PRO, LICENSE_TIER_ENTERPRISE
[2.3.50.2][11/07/2026] Criado LicenseDialogWidget em resources/widgets/LicenseDialogWidget.py:
  - Diálogo modal (QDialog) com QLineEdit para chave, botão 🔑 para validar
  - Grid de labels: Nível da Licença, Data de Expiração, Status (cores: verde=ativa, vermelho=inativa, cinza=sem chave)
  - Botão 🗑️ Apagar Licença (delega para LicenseManager.delete_license())
  - Botão 💾 Salvar (valida via LicenseManager.save_license_key(); se inválida exibe modal warning)
  - Mensagens modais via QgisMessageUtil (modal_info/modal_warning)
  - Logging via LogUtils com ToolKey.SETTINGS
[2.3.50.3][11/07/2026] Adicionado WidgetFactory.create_license_dialog() em core/ui/WidgetFactory.py:
  - Factory method que cria LicenseDialogWidget com iface e parent
[2.3.50.4][11/07/2026] SettingsPlugin refatorado para usar botão + LicenseDialogWidget:
  - Substituído InputFieldsWidget (license_key) por botão "🔑 Gerenciar Licença" via SimpleButtonWidget
  - Botão abre LicenseDialogWidget via WidgetFactory.create_license_dialog()
  - Licença não é mais salva em _save_prefs() — LicenseDialog salva diretamente via LicenseManager
  - Adicionado método _open_license_dialog()
[2.3.50.5][11/07/2026] Criada skill SKILL_LICENSE.md em docs/skills/:
  - Documenta LicenseManager API, LicenseDialogWidget, integração SettingsPlugin, WidgetFactory
  - Preferências utilizadas, fluxos de validação, proibições
[2.3.50.6][11/07/2026] Adicionadas strings de licença em i18n/Strings_pt_BR.py:
  - REG_TITLE, LICENSE_KEY, LICENSE_LEVEL, LICENSE_EXPIRY, LICENSE_STATUS
  - LICENSE_ACTIVE, LICENSE_INACTIVE, LICENSE_SAVE, LICENSE_DELETE, LICENSE_VALIDATE
  - REGISTRY_SAVED_SUCCESS, REGISTRY_DELETED_SUCCESS, REGISTRY_INVALID, REGISTRY_EMPTY
  - LICENSE_DAYS_REMAINING, NO_KEY, LICENSE_OPEN_DIALOG
2.3.51 11/07/2026
[2.3.51.1][11/07/2026] Adicionado atributo license_level em Tool e filtro por licença no ToolRegistry:
  - Adicionado parâmetro `license_level` (default None) em core/model/Tool.__init__()
  - Configurado `license_level=1` para divide_points_by_strips em ToolRegistry._create_tool_list()
  - Adicionado método `_filter_tools_by_license()` em ToolRegistry que remove ferramentas
    cujo license_level é maior que o nível atual da licença (lido via LicenseManager.get_license_info())
  - Filtro executado ao final de ToolRegistry.__init__(), após validação de main_actions
  - SKILL_LICENSE.md atualizado para v4.0.0 com documentação do filtro
2.3.52 11/07/2026
[2.3.52.1][11/07/2026] SaveTemporaryLayersPlugin refatorado — substituído código manual UI (QPushButton, QLabel, QLineEdit, QHBoxLayout) por GridComplexSelector via WidgetFactory.create_grid_complex_selector():
   - Removido _build_output_folder_selector() e handlers _on_browse_folder/_on_suggest_path/_on_open_explorer
   - Removidos imports de QHBoxLayout, QLineEdit, QPushButton, QLabel, ExplorerUtils (morto)
   - Plugin agora usa GridComplexSelector que encapsula 📁, 🛠️, ➡️ com lógica built-in via ComplexSelector
   - Compatibilidade mantida via self.output_grid["output"].edit
[2.3.52.2][11/07/2026] WidgetFactory: create_complex_selector() removido — ComplexSelector é privado do GridComplexSelector.
    - Factory agora expõe APENAS create_grid_complex_selector() como único ponto de acesso
    - import de ComplexSelector removido da WidgetFactory
2.3.53 11/07/2026
[2.3.53.1][11/07/2026] Corrigido ImportError 'Signal' em ComplexSelector.py:
    - Substituído `from qgis.PyQt.QtCore import Signal` por `from qgis.PyQt.QtCore import pyqtSignal`
    - Substituído `pathChanged = Signal(list)` por `pathChanged = pyqtSignal(list)`
    - Causava falha no SaveTemporaryLayersPlugin ao executar ("cannot import name 'Signal' from 'qgis.PyQt.QtCore'")
    - Nenhum outro arquivo no projeto usava `Signal` incorretamente
2.3.54 15/07/2026
[2.3.54.1][15/07/2026] Corrigido erro Qt6 em DefaultProjectsFolderDialog._build_ui():
    - Substituído `QDialogButtonBox.Ok` e `QDialogButtonBox.Cancel` por
      `QDialogButtonBox.StandardButton.Ok` e `QDialogButtonBox.StandardButton.Cancel`
    - Qt5 suporta ambos, Qt6 exige StandardButton. O uso de StandardButton garante
      compatibilidade Qt5/Qt6
    - Corrigido E501 (linhas longas) nos comentários e na criação do QDialogButtonBox
    - Flake8 passou sem erros após correção
[2.3.54.2][15/07/2026] Corrigido salvamento do tamanho da janela no SettingsPlugin ao fechar pelo botão X:
    - Adicionado Preferences.update_tool_prefs() em utils/Preferences.py que faz merge
      seletivo de chaves (update) sem substituir o dicionário inteiro da ferramenta
    - BasePluginMTL.closeEvent() agora usa Preferences.update_tool_prefs() para persistir
      APENAS window_width e window_height em disco, mesmo quando AUTO_SAVE_PREFS_ON_CLOSE=False
    - SettingsPlugin.AUTO_SAVE_PREFS_ON_CLOSE=False impedia que o tamanho da janela
      fosse persistido no disco quando o usuário fechava sem clicar em "Save"
    - Causa raiz: _persist_window_size() atualizava dict em memória, mas nada era salvo
      no disco pois o bloco `if AUTO_SAVE_PREFS_ON_CLOSE` não executava _save_prefs()
     - A correção anterior (save_tool_prefs) sobrescreveria todas as prefs; corrigido para
       update_tool_prefs que preserva as demais configurações da ferramenta
2.3.55 20/07/2026
[2.3.55.1][20/07/2026] Corrigido JsonToVectorTranslator._resolve_geometry não resolvendo coordenadas MRK:
  - Adicionada Tentativa 3 (fallback MRK): quando GpsLatitude/GpsLongitude (EXIF/XMP) não estão disponíveis,
    tenta Lat/Lon do MRK como fallback — soluciona erro "Nenhum registro com geometria válida encontrado"
    em pipelines com source "mrk+photo" cujas fotos não possuem GPS embutido.
  - Causa raiz: _resolve_geometry ignorava o parâmetro source e só tentava campos EXIF/XMP,
    mesmo quando os dados vinham exclusivamente do arquivo MRK.
  - A correção beneficia pipelines com fotos sem EXIF GPS (ex: DJI sem GPS de câmera,
    mas com arquivo .mrk contendo coordenadas).
